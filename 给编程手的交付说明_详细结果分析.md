# 给编程手的交付说明：实现范围、详细结果与论文接口

## 1. 文档用途

本文档用于编程手最终签收和向论文手交接，回答四个问题：程序实际做了什么、正式结果是什么、结果可信到什么程度、论文中可以和不可以怎样表述。

本文档中的数值均来自本目录正式CSV，未根据图形目测、未从题面示例抄录，也未人工补数。若本文档与其他文字材料有冲突，以 `results/` 中正式结果表为准；每条强结论的机器可读索引见 `results/99_summary/claim_evidence.csv`。

交付根目录为：

```text
E:\2022C\C题_完整建模交付_20260830
```

## 2. 编程手完成了什么

编程工作不是简单地“把Excel代入公式”，而是完成了一条可复现的统计建模管线：

1. 从原附件按中文列名读取表单1、表单2和表单3，并计算输入文件哈希。
2. 建立文物、采样点和未知样品三个层级，保留原始行追溯ID。
3. 区分Excel空白与显式零；空白按任务合法分组填补，显式零采用训练集最小正值乘系数进行替代。
4. 对14种化学成分闭合到100%，再进入CLR、ILR或预定义平衡坐标，避免直接对百分比做伪欧氏分析。
5. 所有交叉验证、Bootstrap和置换均以文物为抽样单位，防止同一文物的多个点跨训练折和验证折。
6. 问题2完成前不读取表单3；冻结问题2.1完整管线并保存哈希后，问题3才加载未知样品。
7. 对问题1.1、1.2、1.3、2.1、2.2、3、4分别生成正式结果、误差、灵敏度、稳定性、运行日志和论文图。
8. 完成同配置复跑验收、文件清单、清单完成矩阵以及错误和局限报告。

对应核心脚本如下：

| 脚本 | 职责 |
|---|---|
| `code/00_ingest_preprocess.py` | 数据读取、有效性检查、缺失审计、预处理比较和公共参数落盘 |
| `code/common.py` | 任务感知预处理器、闭合、CLR/ILR、公共读写和哈希函数 |
| `code/10_q1_1_association.py` | 属性与风化的文物级关联分析 |
| `code/11_q1_2_weather_effect.py` | 类型分层的风化成分效应估计 |
| `code/12_q1_3_counterfactual.py` | 风化样品的反事实未风化成分估计 |
| `code/20_q2_1_classification.py` | 三种分类器的嵌套分组CV、比较、校准门禁与冻结 |
| `code/21_q2_2_subclasses.py` | 类型内部的R型筛选、Q型聚类和稳定性判断 |
| `code/30_q3_unknown.py` | 加载冻结管线预测A1—A8，并进行适用域与拒识判断 |
| `code/40_q4_network.py` | 两类玻璃的比例性网络、差异置换和风化调整 |
| `code/run_all.py` | 从原始附件开始的一键完整复跑 |

## 3. 数据口径与预处理结果

### 3.1 样本规模

- 表单1：58件文物，文物编号唯一。
- 表单2：69个采样点。
- 成分原始和满足85%—105%的有效点：67个。
- 有效点涉及56件文物。
- 无效点为采样点15和17，原始成分和分别为79.47%和71.89%。它们未进入主分析，只在预注册敏感性分支中恢复。
- 11件文物具有多个采样点，因此分类、聚类、网络和重采样不能把69个点当成69个独立文物。
- 表单3：A1—A8共8个未知样品，均在问题2冻结后读取。

正式证据：`results/00_audit/schema_and_key_validation.json`、`sample_validity.csv`、`repeated_sampling_audit.csv`。

### 3.2 缺失结构

缺失不是轻微问题。14种成分中，SnO2缺失率为89.86%，SO2为88.41%，Na2O为72.46%，K2O为40.58%，MgO为36.23%，Fe2O3为34.78%。SiO2没有缺失，Al2O3仅缺失1.45%。这说明不同成分的信息密度相差很大，不能把空白简单全部视为数值0，也不能在全数据上一次性填补后再做交叉验证。

主方法使用训练集内部的任务合法分组中位数；若“组×成分”观测数不足5，则回退到当前训练集全局中位数；仍不足时排除连续特征。问题1.2、1.3、2.2和4允许用已知玻璃类型分组，问题2.1只允许使用预测时可见的表面风化状态，问题3只调用问题2.1冻结的填补参数。

人工遮蔽检验中，各方法对已观测值的总体MAE为：合法分组中位数3.8277、KNN(k=5) 4.1697、全局中位数4.5665、全局均值4.7815、直接填0为8.9667个百分点。该实验说明合法分组中位数在当前遮蔽口径下误差最低，但不能据此证明真实未检出机制是随机缺失。

### 3.3 成分约束检查

所有主模型用到的成分在填补和零替代后重新闭合。闭合误差最大约为 `5.68×10^-14`，远低于预注册的 `10^-8` 门槛；所有ILR值均为有限值。这里的微小误差属于浮点运算末位，不是成分和真的偏离100%。

正式证据：`component_audit.csv`、`imputation_mask_validation.csv`、`imputation_method_comparison.csv`、`closure_audit.csv`、`zero_replacement_parameters.csv`。

## 4. 问题1.1：文物属性与表面风化的关系

### 4.1 解决的问题

判断玻璃类型、纹饰和颜色是否与文物表面风化状态存在统计关联。独立单位是58件文物，不使用化学成分，也不把颜色空白推测成某种颜色。

### 4.2 正式结果

| 属性 | 检验 | 原始p值 | BH校正p值 | 校正Cramér's V | 95% Bootstrap区间 | 结论 |
|---|---|---:|---:|---:|---:|---|
| 玻璃类型 | Pearson卡方 | 0.008715 | 0.026144 | 0.3208 | [0, 0.5755] | 在5% FDR下保留关联证据，效应量为中等水平但区间较宽 |
| 纹饰 | 固定边际标签置换10000次 | 0.106289 | 0.159434 | 0.2264 | [0, 0.4329] | 未达到显著门槛 |
| 颜色 | 固定边际标签置换10000次 | 0.315268 | 0.315268 | 0.1506 | [0, 0.5101] | 未达到显著门槛 |

玻璃类型的校正p值小于0.05，因此可以写“不同玻璃类型的表面风化分布存在统计关联”。但Cramér's V区间下界为0，说明58件文物仍不足以把效应大小估计得很精确，不能写成“玻璃类型决定风化”。

纹饰和颜色的p值均未过FDR，不应写“没有任何关系”，只能写“在当前样本和检验功效下未获得足够证据”。

### 4.3 调整Logistic的限制

调整Logistic中出现了近分离：系数标准误极大，OR区间接近从0延伸到极大值。模型虽然数值上收敛，但这些极端OR不具备可解释性。因此问题1.1的核心证据应使用列联检验和Cramér's V；调整Logistic只作为“稀疏类别导致估计不稳定”的诊断证据，不在论文中引用其极端OR数值。

### 4.4 推荐图表

- `q1_1_glass_type_weathering_stacked.pdf`：直观看玻璃类型与风化比例。
- `q1_1_cramers_v_forest.pdf`：比较三个属性的效应量及区间。
- 纹饰、颜色堆叠图可放附录。

正式结果：`results/01_q1/q1_1_association.csv`、`q1_1_contingency.csv`、`q1_1_logistic.csv`、`q1_1_sensitivity.csv`。

## 5. 问题1.2：风化对化学成分的影响

### 5.1 解决的问题

在高钾和铅钡两类内部，分别比较风化点与无风化点的整体组成差异。由于成分总和固定为100%，主模型在ILR空间估计，不对14个原始百分比做互相独立的普通回归。

### 5.2 整体效应

| 玻璃类型 | ILR整体效应范数 | 95% Bootstrap区间 | 文物级置换p值 | 成功Bootstrap | 失败尝试 |
|---|---:|---:|---:|---:|---:|
| 高钾 | 2.7441 | [2.2619, 3.5197] | 0.000300 | 1000 | 37 |
| 铅钡 | 1.9422 | [1.5030, 2.5924] | 0.000100 | 1000 | 54 |

两类的区间均不接近0，置换p值也很小，说明风化状态与两类玻璃的整体相对组成均存在明显统计差异。高钾类的效应范数更大，但两类ILR基底和样本结构不同，不宜把2.7441/1.9422直接解释为“高钾风化强度是铅钡的1.41倍”。

失败尝试均被记录，最终两类各获得1000个成功Bootstrap；失败率远低于10%的降级门槛。

### 5.3 组成层面的主要变化

模型预测的高钾类无风化到风化组成变化中：SiO2约增加22.15个百分点，K2O约减少8.74个百分点，CaO约减少5.65个百分点，Al2O3约减少4.50个百分点，Fe2O3约减少1.36个百分点，P2O5约减少0.63个百分点；这些成分的区间未跨0。MgO、CuO、PbO、BaO和SrO的相应区间跨0，不能强调确定方向。

铅钡类中：SiO2约减少29.93个百分点，PbO约增加24.12个百分点；CaO约增加1.07个百分点。其他成分是否稳定变化必须以 `q1_2_composition_contrast.csv` 的区间为准，不能只看点估计正负。

这些是闭合组成中的相对变化，含义是“相对于其他成分的占比结构改变”，不能直接推断某一化学物质绝对增加、析出或发生了某种反应。

### 5.4 推荐图表

- `q1_2_aitchison_pca_weathering.pdf`：两类风化/无风化样本在组成空间的分布。
- `q1_2_ilr_effect_forest.pdf`：ILR效应及区间。
- `q1_2_composition_change_intervals.pdf`：可直接支持各成分变化描述。
- 留一文物影响图用于稳定性说明，建议放附录。

## 6. 问题1.3：风化样品的反事实未风化成分

### 6.1 解决的问题

问题没有真实的“同一文物风化前后配对值”，因此程序没有训练普通监督恢复模型，而是冻结问题1.2估计的类型内风化效应，在ILR空间对风化点做反向位移，再逆变换为闭合到100%的反事实未风化组成。

### 6.2 正式结果和可信边界

- 共对32个有效风化点生成反事实估计，其中高钾6个、铅钡26个。
- 1000套问题1.2 Bootstrap预处理器和效应参数被传播到区间估计中。
- 反事实组成闭合误差最大为 `5.68×10^-14`，无负数和非有限值。
- Aitchison不确定性均值为1.5302，中位数1.5143，范围为1.3371—1.7667。
- 仅6个点标记为域内；26个点标记为“超出适用域或不可识别”。

“26/32不在可靠适用域”是本问最重要的风险信息。论文不能把32行估计全部当作同等可信的恢复真值。建议正文只展示估计方法、若干代表性域内样品和不确定性范围；完整32行放附录，并显式保留 `applicability_flag`。

由于没有真实配对真值，不能计算也不能声称恢复RMSE、恢复准确率或“还原正确”。允许表述为“基于冻结风化效应的模型反事实估计”。

### 6.3 推荐图表

- `q1_3_observed_vs_counterfactual_dumbbell.pdf`：观测与反事实估计对比。
- `q1_3_counterfactual_migration_arrows.pdf`：组成空间中的反向迁移。
- `q1_3_largest_uncertainty_intervals.pdf` 和 `q1_3_uncertainty_ranking.pdf`：突出估计不确定性与适用边界。

## 7. 问题2.1：玻璃类型分类模型

### 7.1 解决的问题和防泄漏设计

用56件具有有效成分点的已知文物区分高钾与铅钡。候选模型为可解释余额Logistic、CART和收缩LDA。采用5折×20次重复的外层分层分组CV，内层4折调参；同一文物的多个点始终在同一折。填补、零替代、闭合、余额特征和标准化全部只在训练折拟合。

### 7.2 样本外结果

| 模型 | 平衡准确率均值±SD | 范围 | Macro-F1均值 | ROC-AUC均值 | Brier均值 |
|---|---:|---:|---:|---:|---:|
| Logistic | 0.7825±0.0368 | 0.7125—0.8688 | 0.7768 | 0.8355 | 0.1492 |
| LDA | 0.7806±0.0076 | 0.7625—0.7875 | 0.7992 | 0.8238 | 0.1318 |
| CART | 0.7541±0.0267 | 0.7125—0.7938 | 0.7598 | 0.7770 | 0.1599 |

Logistic的平均平衡准确率和ROC-AUC最高，因此最终冻结Logistic。LDA的Brier和Macro-F1更好、跨重复波动更小，说明Logistic并非所有指标都绝对最优；论文应写“综合样本外区分能力、解释性和预注册选择规则冻结Logistic”，不能写“Logistic在所有指标上显著优于其他模型”。

### 7.3 概率校准判断

校准器只用第1次重复拟合，用其余19次评估。启用条件为Brier至少改善0.01且平衡准确率下降不超过0.01。实际Brier改善为：CART 0.00949、LDA 0.00467、Logistic 0.00648，均未达到0.01，因此三个模型都不启用额外校准，冻结阈值保持0.5。

这不是“忘记校准”，而是正式门禁认为当前改进幅度不足，强行校准反而会增加流程复杂度。

### 7.4 灵敏度结果

在统一5折灵敏度口径下，主方案平衡准确率为0.7875。零替代系数改为0.25或0.75仍为0.7875；成分正值±5%或±10%扰动后也为0.7875。全局中位数和KNN分支为0.8000，纳入15、17后为0.8083。

这些结果表明分类标签对零替代和小幅输入扰动稳定，但预处理方法和是否纳入无效点会带来约1.25—2.08个百分点变化。KNN或全局中位数在这一单次5折灵敏度上更高，不足以推翻主方案：主方案还受防泄漏、可部署分组语义和统一预注册约束。论文不得删除这些对主方案不利的比较。

### 7.5 模型冻结

最终模型、敏感性管线和1000个Bootstrap管线保存在 `models/q2_1_frozen_pipeline/`。`manifest.json`记录输入哈希、活动成分、余额定义、最终参数和模型文件哈希；`FROZEN_BEFORE_TABLE3.txt`证明表单3在冻结前未被读取。

### 7.6 推荐图表

- `q2_1_model_performance_distribution.pdf`：三模型重复CV指标分布。
- `q2_1_roc_and_calibration.pdf`：ROC与校准表现。
- `q2_1_selected_model_confusion_matrix.pdf`：冻结模型的文物级混淆情况。
- `q2_1_feature_stability.pdf`：余额特征选择稳定性。
- CART规则图只作为可解释基准，不是最终模型。

## 8. 问题2.2：类型内部亚类探索

### 8.1 解决的问题

在已知高钾和铅钡类型内部寻找可能的成分亚类。程序先在CLR/ILR空间合并同一文物的多个点，得到文物Aitchison中心；用R型变量筛选减少冗余，再对文物进行Ward聚类，并与KMeans和GMM比较。

### 8.2 候选划分

- 高钾：16件文物，探索性候选 `k=2`；Ward轮廓系数0.4541，最小簇4件，预测强度1.0，但Bootstrap ARI中位数仅0.4776，10%分位数为负。
- 铅钡：40件文物，探索性候选 `k=6`；大量文物的分配频率偏低，35/40被标记为边界文物，平均分配频率约0.6198。

高钾虽然部分内部指标较好，但重采样ARI不足；铅钡的边界文物比例极高。联合稳定性门槛最终对两类都给出 `stable_subclass_supported=0`。

因此，`selected_k`只是为了生成探索性画像而保留的候选k，不等于数据证实存在2个或6个稳定亚类。论文允许写“探索性聚类提示若干可能分组，但稳定性检验不支持确立正式亚类”；禁止写“聚类准确率”或把簇号当真值标签。

### 8.3 灵敏度

高钾候选划分对多数场景稳定，但R型阈值0.6时ARI降至0.339。铅钡候选划分对R型阈值和±5%/±10%输入扰动敏感，ARI可降至0.195—0.574。该结果进一步支持“只作探索性分组”的降级结论。

### 8.4 推荐图表

- `q2_2_q_artifact_dendrograms.pdf`：文物层次聚类结构。
- `q2_2_k_metrics_and_stability.pdf`：候选k与稳定性门槛。
- `q2_2_consensus_matrices.pdf`：重采样共识结构。
- `q2_2_subclass_profiles_heatmap.pdf`：探索性簇画像，仅在图注中标明“不构成稳定亚类证据”。

## 9. 问题3：A1—A8未知样品分类

### 9.1 解决的问题

在完全不重新调参、不重新计算填补参数的前提下，用问题2.1冻结管线判断8个未知样品的类型倾向，同时报告Bootstrap概率区间、模型一致性、适用域和拒识原因。

### 9.2 正式结果

| 样品 | Logistic倾向 | 铅钡概率 | 95% Bootstrap区间 | 多数标签频率 | 三模型一致 | 拒识 | 解释 |
|---|---|---:|---:|---:|---|---|---|
| A1 | 高钾 | 0.3973 | [0.0753, 0.7206] | 0.721 | 否 | 是 | 区间跨阈值、频率不足0.8、三模型不一致 |
| A2 | 铅钡 | 0.7931 | [0.6910, 0.9199] | 1.000 | 是 | 否 | 相对稳定 |
| A3 | 铅钡 | 0.6636 | [0.4936, 0.8488] | 0.970 | 是 | 是 | 区间跨0.5阈值 |
| A4 | 铅钡 | 0.7271 | [0.5954, 0.8752] | 0.996 | 是 | 否 | 相对稳定 |
| A5 | 高钾 | 0.4991 | [0.1563, 0.7596] | 0.531 | 否 | 是 | 近阈值、区间跨阈值、频率低、三模型不一致 |
| A6 | 铅钡 | 0.8937 | [0.7567, 0.9778] | 1.000 | 是 | 否 | 相对稳定 |
| A7 | 铅钡 | 0.8213 | [0.6275, 0.9502] | 0.994 | 是 | 否 | 相对稳定 |
| A8 | 铅钡 | 0.9396 | [0.7699, 0.9947] | 0.998 | 是 | 否 | 相对稳定 |

主模型给出A1、A5偏高钾，其余6个偏铅钡。但最终交付不能只抄这一列：A1、A3、A5触发拒识，必须同时交付拒识标记和原因。较稳妥的强结论对象是A2、A4、A6、A7、A8。

所有8个样品都未被几何适用域规则判为域外；拒识主要来自概率区间、Bootstrap标签频率、模糊区和三模型不一致。

零替代系数0.25/0.75没有改变任何标签。使用全局中位数时A1会从高钾变为铅钡，进一步说明A1结论依赖预处理口径，拒识是必要的。未知样品没有真值，因此不得生成或声称问题3准确率。

### 9.3 推荐图表

- `q3_unknown_probabilities_intervals.pdf`：8个样品概率和区间，是正文首选。
- `q3_bootstrap_label_frequency.pdf`：说明标签稳定性。
- `q3_three_model_consistency.pdf`：突出A1、A5的模型分歧。
- `q3_applicability_projection.pdf`：展示未知样品与训练域关系。

## 10. 问题4：两类玻璃成分关系网络

### 10.1 解决的问题

研究两类玻璃内部成分之间的相对协同/替代关系，并检验两类关系是否不同。主分析使用文物Aitchison中心和比例性系数，不把原始百分比Pearson相关当核心证据。

### 10.2 网络估计和差异检验

两类各完成1000次文物Bootstrap，差异检验完成10000次保持组大小的文物级类型置换。45条共同候选边统一做BH-FDR；核心差异边还必须同时满足 `|Δ|≥0.20` 和方向Bootstrap一致率≥0.80。

最终共有4条核心差异边：

| 成分对 | 高钾ρp | 铅钡ρp | Δ=高钾−铅钡 | 95%区间 | FDR q | 方向频率 |
|---|---:|---:|---:|---:|---:|---:|
| SiO2—Al2O3 | -0.3478 | 0.4195 | -0.7673 | [-0.9487, -0.5502] | 0.01125 | 1.0000 |
| SiO2—PbO | -0.0620 | -0.7056 | 0.6435 | [0.4002, 0.9013] | 0.01800 | 1.0000 |
| MgO—CuO | 0.4948 | -0.1299 | 0.6247 | [0.2196, 0.8899] | 0.02700 | 0.9958 |
| Al2O3—PbO | 0.2049 | -0.5166 | 0.7214 | [0.2191, 0.8992] | 0.00450 | 0.9937 |

这些结果表明两类玻璃在上述成分对的相对变化结构上存在稳定差异。它们不能直接解释为化学反应、原料配方、制作工艺或因果路径；任何工艺解释都需要外部考古化学文献支持。

### 10.3 稳定性与限制

高钾主网络在零替代和多数检测阈值分支中较稳定；铅钡网络对零替代、检测率和边阈值更敏感，边集Jaccard约0.25—0.50。因此，4条经过联合门槛的差异边可作为核心统计结果，但单类完整网络中的普通边不应全部作强解释。

高钾类不满足预注册的 `n/p≥5` 条件，Graphical Lasso被自动禁用；程序没有降低门槛凑图。收缩偏相关仍作为基准保留在 `q4_partial_correlation_baseline.csv`。

### 10.4 推荐图表

- `q4_core_proportionality_networks.pdf`：两类主网络，同一布局和尺度。
- `q4_core_differential_network.pdf`：只画4条通过联合门槛的核心差异边。
- `q4_effect_vs_selection_frequency.pdf`：解释效应量与稳定性筛选。
- `q4_weathering_adjustment_comparison.pdf`：风化调整前后对比。
- 比例性矩阵热图可放附录。

## 11. 跨问题综合判断

### 11.1 可以形成的主线

1. 文物玻璃类型与表面风化存在统计关联，而纹饰和颜色证据不足。
2. 在两类玻璃内部，风化都对应显著的整体组成结构变化，但变化方向和主要成分不同。
3. 基于问题1.2可构造风化样品的反事实未风化估计，但大部分样品适用域不足，结果必须带区间和域标记。
4. 已知文物类型可用组成余额进行约0.78平衡准确率的样本外分类；Logistic被正式冻结，但LDA在部分指标上更优。
5. 当前数据不足以确立稳定的类型内部亚类。
6. A2、A4、A6、A7、A8的铅钡倾向较稳定；A1、A3、A5应拒识或保留不确定表述。
7. 两类玻璃存在4条通过FDR、效应量和稳定性联合门槛的核心比例关系差异边。

### 11.2 全局最容易犯的错误

- 把69个点写成69个独立文物。
- 把Excel空白全部改成0。
- 用表单3参与问题2调参。
- 把问题1.3反事实估计写成真实恢复值。
- 只报Logistic最高均值，不报LDA的Brier和稳定性优势。
- 把问题2.2候选k写成已证实的亚类数。
- 忽略A1、A3、A5拒识标记。
- 把网络边解释成因果、配方或化学反应。

## 12. 论文手应如何使用交付物

建议按以下顺序取数：

1. 先读本文档确定每问可以下什么结论。
2. 从 `results/99_summary/claim_evidence.csv` 找到结论对应的结果文件和字段。
3. 正文数值直接引用正式CSV，统一保留合理小数位；不要从PDF图中反读数值。
4. 图表从 `figures/` 插入；图的源数据从 `figures/source_data/` 查验。
5. 灵敏度、误差和稳定性分别从各问的 `*_sensitivity.csv`、Bootstrap/置换表和重复CV表提取，不混成一个“模型稳健”结论。
6. 在模型评价或局限章节明确写入 `reports/ERRORS_AND_LIMITATIONS.md` 中的限制。

每问最小论文接口如下：

| 子问题 | 正文核心表 | 正文核心图 | 附录材料 |
|---|---|---|---|
| 1.1 | `q1_1_association.csv` | 类型堆叠图、V森林图 | 列联表、Logistic诊断、敏感性 |
| 1.2 | `q1_2_overall_effect.csv`、`q1_2_composition_contrast.csv` | PCA、成分变化区间 | ILR系数、残差、留一和Bootstrap失败 |
| 1.3 | `q1_3_counterfactual_long.csv` | 哑铃图、迁移图 | 32点完整估计、域标记和敏感性 |
| 2.1 | `q2_1_model_metrics.csv`、校准表 | 模型分布、ROC/校准 | CV划分、OOF、参数、系数和冻结manifest |
| 2.2 | `q2_2_cluster_metrics.csv`、`q2_2_subclasses.csv` | k稳定性、共识矩阵 | 探索簇标签、画像和敏感性 |
| 3 | `q3_unknown_predictions.csv` | 概率区间图 | Bootstrap明细、适用域和敏感性 |
| 4 | `q4_differential_edges.csv` | 核心差异网络 | 完整边表、风化调整、偏相关和网络稳定性 |

## 13. 复现与验收状态

一键运行命令：

```powershell
cd E:\2022C\C题_完整建模交付_20260830
.\.venv\Scripts\python.exe code\run_all.py
```

最终门禁结果：

- 正式结果CSV共67个，没有零字段空表。
- 论文PDF图35张，全部可读取，每张1页。
- 文件清单记录197个交付文件，不把 `.venv/` 第三方依赖冒充成果。
- 复跑比较64个非汇总核心CSV：63个严格语义哈希一致；1个有效性表的差异只涉及Excel重复读取的浮点末位，并通过 `10^-12` 逐单元格容差复核。
- 问题2.1最终1000个Bootstrap管线全部成功；问题4完成10000次有效类型置换。

详细验收入口：

- `reports/RESULTS_REPORT.md`
- `reports/CHECKLIST_COMPLETION_MATRIX.md`
- `reports/FILE_INVENTORY.csv`
- `reports/ERRORS_AND_LIMITATIONS.md`
- `reports/REPRODUCIBILITY_CHECK.md`

## 14. 编程手最终签收结论

本交付已经覆盖统一数据审计和7个子任务的主计算、结果表、敏感性、稳定性、论文图和复现材料。需要降级或保留的结论也已明确落盘：问题1.1调整Logistic近分离、问题1.3多数反事实点适用域不足、问题2.2不支持稳定亚类、问题3有3个拒识样品、问题4的Graphical Lasso受样本量门禁禁用。

签收时不应把这些限制视为“没做完”，而应视为程序依据预注册规则拒绝输出过强结论。真正尚未完全满足的实现细项只有CART采用预注册 `ccp_alpha={0,0.005,0.01}` 网格，而不是每折动态生成剪枝路径；该偏离已在错误与局限报告中保留，不能删除。
