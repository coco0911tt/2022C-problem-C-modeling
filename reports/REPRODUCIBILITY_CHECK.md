# 复现性核验

- 比较范围：所有非99_summary正式CSV，列和行排序后的语义SHA-256；浮点复核容差1e-12
- 复跑前：64 个 CSV；复跑后：64 个 CSV
- 严格哈希一致：63；严格哈希不一致：1
- 结论：通过

不一致文件：

- `results/00_audit/sample_validity.csv`

浮点容差复核：严格哈希差异仅来自Excel重复读取后的浮点末位，逐单元格最大绝对差=0.000e+00，低于1e-12；类别、ID、有效标记和排除原因一致。
