# 文件清单补充说明（2026-08-31）

## 新增文件

| 相对路径 | 类型 | 作用 | 属性 | 对应交付项 |
|---|---|---|---|---|
| `给编程手的交付说明_详细结果分析.md` | Markdown | 向编程手和论文手说明全部实现、正式结果、误差、灵敏度、稳定性、限制和论文接口 | UTF-8；约24 KB；14个二级章节；所有数字回链正式CSV | 编程手最终签收、详细结果分析、论文接口 |

## 为什么使用补充清单

生成新文档后尝试重新运行 `code/99_finalize.py` 更新 `reports/FILE_INVENTORY.csv`，但该CSV当时被其他程序占用，Windows返回 `PermissionError: [Errno 13] Permission denied`。为避免覆盖、绕过文件锁或隐瞒错误，保留原文件清单并以本文档记录新增文件。

该文件锁只影响文件清单的再次写入，不影响建模脚本、正式结果CSV、PDF图表、新增交付说明或既有复现结论。
